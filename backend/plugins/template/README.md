# Template Plugin

This is a template Jarvis plugin. To create a new plugin, simply copy this folder and rename it.

```bash
cp -r backend/plugins/template backend/plugins/my_new_plugin
```

Then edit `__init__.py` to define your own tools and logic!

See `docs/SDK.md` for full instructions.
